# Script พรีเซนต์ — การพัฒนาระบบท่อส่งข้อมูลอัตโนมัติและคลังข้อมูล
**เวลารวม: ~20 นาที | 42 สไลด์**

> ## ⚠️ ปรับปรุง 5 ก.ย. 2026 — อ่านก่อนใช้
>
> ไฟล์นี้เขียนเมื่อ ก.ค. 2026 เนื้อหาบางส่วนไม่ตรงกับระบบและผลการวิจัยปัจจุบัน
>
> | ประเด็น | เดิม | ปัจจุบัน |
> |---|---|---|
> | URL เว็บบน Cloud Run | ใช้งานได้ | **ตอบ HTTP 500** — แคปหน้าจอจาก `http://localhost:8000` แทน |
> | จำนวนสินทรัพย์ | 7 | **30** |
> | หน้าต่างข่าว | 168 ชม. (7 วัน) | **2,160 ชม. (90 วัน)** |
> | แท็บ Dashboard | 5 (มี Heatmap) | **7** (ถอด Heatmap เพิ่ม Signal, Fundamentals) |
> | ผล Correlation | "รอข้อมูลสะสมแล้วจะสรุปได้" | **ข้อมูลพอแล้ว — ผลเป็นลบ** |
>
> ข้อสรุปปัจจุบัน: ทั้ง sentiment และงบการเงิน **ไม่แสดงความสัมพันธ์กับผลตอบแทน**
> ที่รอดการตรวจสอบ 3 ชั้น (ค่าผิดปกติ / นอกกลุ่มตัวอย่าง / ขนาดตัวอย่างจริง)
> รายละเอียดใน `thesis_methodology_revised.md`

---


> 💡 **วิธีใช้:** ตัวเอียงคือ note ช่วยเตือน, ตัวปกติคือบทพูด

---

## หน้า 1 — Title
*[รอกรรมการพร้อม แล้วเริ่ม]*

"สวัสดีครับ ผม นายเอเชีย อ่อนพรม นักศึกษาสาขาวิทยาการข้อมูลและการวิเคราะห์เชิงธุรกิจ สจล. ครับ

วันนี้จะนำเสนอโครงงานชื่อ **'การพัฒนาระบบท่อส่งข้อมูลอัตโนมัติและคลังข้อมูล เพื่อวิเคราะห์ความสัมพันธ์ระหว่างข่าวเศรษฐกิจและราคาหลักทรัพย์'** ครับ

ขอเริ่มเลยนะครับ"

---

## หน้า 2 — ที่มาและปัญหา
"ก่อนอื่น ขอเล่าที่มาของโครงงานก่อนครับ

ปัจจุบันนักลงทุนรายย่อยมีสัดส่วนมูลค่าซื้อขายสูงถึง **28.98%** ของตลาดหลักทรัพย์ไทย ซึ่งถือว่าเยอะมากครับ แต่ปัญหาคือ ข่าวสารทางการเงินมีปริมาณมหาศาล อัปเดตรวดเร็ว และกระจายอยู่หลายแหล่ง

มนุษย์ไม่สามารถติดตามข่าวหลายแหล่งพร้อมกันและวิเคราะห์ได้ทันเวลา ส่งผลให้เกิด **Information Lag** — คือรู้ช้ากว่าตลาด และเกิด **Bias** — คือตัดสินใจด้วยอารมณ์แทนข้อมูล

โครงงานนี้จึงถูกสร้างขึ้นเพื่อแก้ปัญหานี้ครับ"

---

## หน้า 3 — วัตถุประสงค์
"โครงงานนี้มีวัตถุประสงค์ 5 ข้อครับ

หนึ่ง พัฒนาระบบ ETL Pipeline ที่ดึงราคาสินทรัพย์และข่าวได้อัตโนมัติ
สอง ประยุกต์ใช้โมเดล FinBERT วิเคราะห์ Sentiment ของข่าวการเงิน
สาม ออกแบบ Data Warehouse แบบ Star Schema บน PostgreSQL
สี่ Deploy ระบบบน Google Cloud Platform แบบ Serverless
และห้า พัฒนา Web Dashboard สำหรับ Visualization เพื่อสนับสนุนการตัดสินใจ

ทั้ง 5 ข้อนี้เชื่อมกันเป็นระบบครบวงจรครับ"

---

## หน้า 4 — ขอบเขตของโครงงาน
"ขอบเขตของโครงงานครับ

ด้านสินทรัพย์ ระบบครอบคลุม **30 รายการ** ได้แก่ หุ้นสหรัฐ 21 ตัว ดัชนี 3 ตัว คริปโท 2 ตัว ค่าเงิน 2 คู่ และสินค้าโภคภัณฑ์ 2 รายการ

*[note: เริ่มต้นที่ 7 รายการ ขยายเป็น 30 ซึ่งมีผลโดยตรงต่อข้อสรุป]*

แหล่งข้อมูลราคาหลักคือ Yahoo Finance โดยมี Stooq เป็น Failover อัตโนมัติ ส่วนข่าวดึงจาก Google News RSS Feed

ด้าน Infrastructure ทั้งหมด Deploy บน GCP ครับ ทั้ง Cloud Run, Cloud SQL, และ Cloud Scheduler และมีระบบส่ง Daily Report ทางอีเมลด้วย"

---

## หน้า 5 — ประโยชน์ที่คาดว่าจะได้รับ
"ประโยชน์ที่คาดว่าจะได้รับมี 3 ข้อหลักครับ

หนึ่ง **Market Insight ที่รวดเร็ว** — ผู้ใช้เข้าถึง Sentiment รายสินทรัพย์ผ่าน Dashboard โดยไม่ต้องอ่านข่าวเองทุกชิ้น

สอง **Zero-Touch Automation** — ระบบรันเองทุกวันโดยอัตโนมัติ ลด Human Error

และสาม **ลด Bias ในการตีความข้อมูล** — ใช้โมเดล AI แทนการตัดสินใจเชิงอารมณ์ของมนุษย์ครับ"

---

## หน้า 6 — การวิเคราะห์ความรู้สึกในตลาดการเงิน
"เข้าสู่บทที่ 2 ทบทวนวรรณกรรมและทฤษฎีที่เกี่ยวข้องครับ

เริ่มจากทฤษฎีพื้นฐาน — **Efficient Market Hypothesis (EMH)** บอกว่าราคาสะท้อนข้อมูลสาธารณะทันที แต่ในความเป็นจริง ข่าวไม่ได้ส่งผลทุกข่าว ต้องแยก signal ออกจาก noise ให้ได้

มีงานวิจัยสนับสนุนทิศทางนี้ครับ เช่น Tetlock (2007) พบว่าคำเชิงลบใน Wall Street Journal สัมพันธ์กับการลดของ Dow Jones และ Bollen et al. (2011) พบว่า Twitter mood พยากรณ์ทิศทาง DJIA ได้แม่นยำถึง 87.6%

**Sentiment Analysis** หรือ Opinion Mining คือการใช้ AI วัด 'ทัศนคติ' ในข้อความ ซึ่งไม่ใช่แค่ บวก/ลบ แต่ยังให้ Confidence Score ด้วยครับ"

---

## หน้า 7 — Transformer / BERT / FinBERT
"มาดูโมเดลที่ใช้ครับ

ทั้งหมดสร้างบนฐานของ **Transformer** — สถาปัตยกรรมที่ใช้กลไก Self-Attention เรียนรู้ความสัมพันธ์ระหว่างคำในประโยคได้ดีกว่า RNN/LSTM รุ่นก่อน

**BERT** พัฒนาต่อยอดจาก Transformer โดย Google ใช้ Masked Language Modeling ทำให้เข้าใจบริบทได้ทั้งซ้ายและขวาของคำ

และ **FinBERT** คือ BERT ที่ถูก Fine-tune ด้วยข้อมูลข่าวการเงินโดยเฉพาะ จำแนกอารมณ์ข่าวเป็น Positive, Negative, Neutral และให้ผลแม่นยำกว่าโมเดลภาษาทั่วไปในบริบทการเงินครับ"

---

## หน้า 8 — ตัวอย่างความหมายของคำ
*[ชี้ที่ภาพ]*

"สไลด์นี้แสดงให้เห็นว่าทำไมต้องใช้ FinBERT แทน BERT ทั่วไปครับ

คำเดียวกันในบริบทการเงินอาจมีความหมายต่างออกไป เช่น คำว่า 'shares' ในภาษาทั่วไปแปลว่า 'แบ่งปัน' แต่ในบริบทการเงินหมายถึง 'หุ้น' FinBERT เข้าใจความแตกต่างนี้ครับ"

---

## หน้า 9 — ETL Pipeline (ทฤษฎี)
"ต่อมาคือ ETL Pipeline ครับ ซึ่งเป็นหัวใจของระบบ

ETL แบ่งเป็น 3 ขั้น — **Extract** คือดึงข้อมูลดิบจากหลายแหล่ง, **Transform** คือแปลงให้อยู่ในรูปแบบมาตรฐาน, และ **Load** คือบันทึกลงฐานข้อมูลปลายทาง

หลักการออกแบบที่สำคัญมี 3 ข้อครับ คือ **Idempotency** — รันซ้ำกี่ครั้งก็ได้ผลเดิม, **Fault Tolerance** — ระบบยังทำงานได้แม้แหล่งข้อมูลบางส่วนขัดข้อง, และ **Automation** — ลด Human Error และรับประกัน Data Continuity ครับ"

---

## หน้า 10 — Data Engineering + RSS Feed
"วิศวกรรมข้อมูลคือศาสตร์ที่มุ่งออกแบบและบำรุงรักษาโครงสร้างพื้นฐาน ให้ข้อมูลไหลจากต้นทางไปยังปลายทางได้อย่างน่าเชื่อถือครับ

ส่วนแหล่งข้อมูลข่าวที่ใช้คือ **RSS Feed** ซึ่งเป็น XML-based protocol มาตรฐานสำหรับเผยแพร่ข่าวบนอินเทอร์เน็ต มีโครงสร้างชัดเจนแต่เนื้อหาข้อความยังเป็น unstructured ดังนั้นต้องผ่านกระบวนการ parsing และ filtering ก่อนนำไปใช้ครับ"

---

## หน้า 11 — Star Schema (Data Warehouse vs Database)
"ฐานข้อมูลที่ใช้เก็บข้อมูลในโครงงานนี้ไม่ใช่ Database ทั่วไปครับ

Database ทั่วไปหรือ OLTP ออกแบบมาเพื่อบันทึกธุรกรรมให้เร็วและถูกต้อง แต่โครงงานนี้ต้องการ **Data Warehouse แบบ OLAP** ซึ่งออกแบบมาเพื่อวิเคราะห์ข้อมูลย้อนหลังในปริมาณมาก

เนื่องจาก Dashboard ต้องอ่านข้อมูลตลอดเวลาแต่เขียนแค่วันละครั้ง OLAP จึงเหมาะสมกว่า

ใน Star Schema มี **Fact Table** เก็บเหตุการณ์ที่เกิดซ้ำๆ เช่น ราคารายวัน, และ **Dimension Table** เก็บบริบทอธิบายที่ไม่ค่อยเปลี่ยน เช่น ชื่อสินทรัพย์ครับ"

---

## หน้า 12 — Star Schema vs ทางเลือกอื่น
*[ชี้ที่ตาราง]*

"เปรียบเทียบ 3 รูปแบบครับ — 3NF, Star Schema, และ Snowflake Schema

Star Schema ถูกเลือกเพราะมีจำนวน JOIN น้อยที่สุด Query เร็วที่สุดสำหรับ Dashboard และเข้าใจโครงสร้างง่าย

การแลกรับ Data Redundancy เล็กน้อยเพื่อได้ Query Performance สูงถือเป็นการตัดสินใจทางสถาปัตยกรรมที่เหมาะสมสำหรับ Use Case นี้ครับ"

---

## หน้า 13 — Cloud Computing & Serverless
"สำหรับ Infrastructure ครับ Cloud Computing เปลี่ยนรูปแบบต้นทุนจาก CAPEX คือซื้อ Server จ่ายก้อน ไปเป็น OPEX คือจ่ายตามใช้งานจริง

และ **Serverless Architecture** ทำให้เราไม่ต้องจัดการ Server, OS, หรือ Scaling เอง ผู้ให้บริการ Cloud ดูแลทั้งหมด เราโฟกัสแค่โค้ดได้เลยครับ"

---

## หน้า 14 — GCP Services
*[ชี้ที่ตาราง]*

"GCP Services ที่ใช้มี 5 ตัวหลักครับ

**Cloud Run** รัน Web Dashboard โดยไม่ต้องดูแล Server, **Cloud Run Job** รัน ETL Pipeline แบบ Batch, **Cloud SQL** คือ PostgreSQL บน Cloud, **Cloud Scheduler** คือ Cron Job ระดับ Enterprise ที่ Trigger ETL ตามเวลา และ **Artifact Registry** เก็บ Docker Image ก่อน Deploy ครับ"

---

## หน้า 15 — Docker Containerization
"Docker คือเครื่องมือที่บรรจุโปรแกรม, dependencies, และ runtime ทั้งหมดไว้ใน Container Image เดียว ทำให้รันได้เหมือนกันทุก environment ไม่มีปัญหา 'ทำงานบนเครื่องตัวเองได้แต่บน Cloud ไม่ได้'

สำคัญมากสำหรับโครงงานนี้ครับ เพราะ FinBERT มี dependencies ซับซ้อน อย่าง PyTorch และ Transformers

นอกจากนี้ยังแยก Image ได้ 2 แบบ — Image สำหรับ Web ที่ Lightweight และ Image สำหรับ ETL ที่รัน FinBERT ซึ่ง Heavyweight กว่า เพื่อจัดการทรัพยากรได้อย่างเหมาะสมครับ"

---

## หน้า 16 — System Architecture Overview
*[ชี้ที่ภาพ Diagram]*

"เข้าสู่บทที่ 3 วิธีการดำเนินการวิจัยครับ

สไลด์นี้แสดงสถาปัตยกรรมโดยรวมของระบบทั้งหมด มองจากภาพรวม จะเห็นว่าข้อมูลเริ่มต้นจากแหล่งภายนอก ไหลผ่าน ETL Pipeline, เข้าสู่ Data Warehouse บน Cloud SQL และแสดงผลผ่าน Web Dashboard พร้อมกับระบบแจ้งเตือนทางอีเมลครับ

ทุกส่วนทำงานอัตโนมัติโดย Cloud Scheduler ครับ"

---

## หน้า 17 — ETL Pipeline Output
"ETL Pipeline ของระบบมี 8 ขั้นตอน โดยแต่ละขั้นมี Output ที่ชัดเจนครับ

เริ่มจากดึงราคา OHLCV ของ 30 สินทรัพย์ → ดึงข่าวจาก RSS → ทำความสะอาดข้อมูล → วิเคราะห์ Sentiment ด้วย FinBERT ทีละชิ้น → Aggregate เป็น Sentiment Index รายวัน → Load ลง 3 Fact Tables และ 3 Dimension Tables → ตรวจสอบ Data Quality → และส่ง Email Report สรุปผล

ทั้ง 8 ขั้นนี้รันต่อเนื่องกันใน Pipeline ครั้งละไม่เกิน 5 นาทีครับ"

---

## หน้า 18 — โมดูลสกัดข้อมูลราคา
"โมดูลแรกคือการดึงราคาสินทรัพย์ครับ

แหล่งข้อมูลหลักคือ Yahoo Finance ดึงผ่าน yfinance library ฟรีและไม่ต้อง API Key แต่ถ้า Yahoo Finance ล้มเหลว ระบบจะ **สลับไป Stooq อัตโนมัติ** โดยผู้ใช้ไม่รู้สึกถึงความขัดข้องเลยครับ

หลังดึงข้อมูลมาแล้ว มีการทำ Data Cleansing — ปรับชื่อคอลัมน์ให้เป็นมาตรฐาน, ลบ null ที่ไม่สมเหตุสมผล, และคำนวณ pct_change รายวัน ซึ่งเป็นตัวแปรหลักสำหรับ Correlation Analysis ครับ"

---

## หน้า 19-20 — โมดูลสกัดข้อมูลข่าว
"โมดูลต่อมาคือการดึงข่าวจาก Google News RSS Feed ครับ

ดึงข่าวค้นหาตาม Ticker Symbol เลย เช่น AAPL, BTC-USD เป็นต้น

แต่ไม่ได้เอาข่าวทุกชิ้นมาครับ มี **Filtering Pipeline 4 ขั้น** — หนึ่ง Age Filter ตัดข่าวเก่ากว่า 7 วัน, สอง Source Credibility Filter เก็บเฉพาะแหล่งน่าเชื่อถือ เช่น Bloomberg, Reuters, CNBC, สาม Timestamp Normalization แปลงทุก format เป็น UTC มาตรฐาน, และสี่ Deduplication ป้องกันข่าวซ้ำด้วย UNIQUE Constraint บน Database ครับ"

---

## หน้า 21 — โมดูล Sentiment ด้วย FinBERT
"ข่าวที่ผ่านการกรองแล้วจะถูกส่งเข้า FinBERT ทีละชิ้นครับ

โมเดลจะให้ผลเป็น Label คือ Positive, Negative, หรือ Neutral พร้อม Confidence Score

จากนั้น Aggregate ผลของข่าวทุกชิ้นในวันนั้นสำหรับสินทรัพย์แต่ละตัว เพื่อสร้าง **Sentiment Index รายวัน** เก็บใน `fact_sentiment_daily`

การ Aggregate ช่วยลดผลกระทบจากข่าวชิ้นเดียวที่ Extreme เกินไป และจับคู่กับราคาปิดรายวันได้ธรรมชาติครับ"

---

## หน้า 22 — โมดูลโหลดข้อมูล
"โมดูล Load ทำหน้าที่บันทึกข้อมูลลง PostgreSQL บน Cloud SQL ครับ

เครื่องมือที่ใช้คือ **SQLAlchemy ORM** ซึ่งเป็นตัวกลางระหว่าง Python กับ Database ช่วยให้โค้ดยืดหยุ่นและอ่านง่าย

หลักการสำคัญของโมดูลนี้คือ **Idempotency** ครับ — ระบบสามารถรัน ETL ซ้ำกี่รอบก็ได้ ผลลัพธ์ในฐานข้อมูลยังคงเหมือนเดิม ไม่มีข้อมูลซ้ำซ้อน เพราะใช้ UPSERT pattern

คุณสมบัตินี้สำคัญมากสำหรับระบบ Production เพราะ ETL อาจต้อง rerun หลังเกิด Error ได้ตลอดครับ"

---

## หน้า 23 — ระบบตรวจสอบคุณภาพข้อมูล
"หลังจาก Load เสร็จแล้ว ระบบจะรัน **Data Quality Checks** อัตโนมัติครับ

มีเกณฑ์ตรวจสอบ 5 ข้อ — **Completeness** ครบ 30 สินทรัพย์ไหม, **Non-zero News** มีข่าวถูกดึงเข้ารอบนี้ไหม, **Null Check** ไม่มี null ในคอลัมน์สำคัญ, **Range Validation** pct_change อยู่ในช่วงสมเหตุสมผลไหม, และ **Deduplication** ไม่มีข่าวซ้ำหลุด

ถ้าไม่ผ่านเกณฑ์ ระบบจะ Flag และแจ้ง Alert ทางอีเมลทันทีครับ"

---

## หน้า 24 — Database Design
*[ชี้ที่ Diagram]*

"นี่คือ Star Schema ของระบบครับ มี Fact Table 3 ตาราง ได้แก่ `fact_price_daily`, `fact_news`, และ `fact_sentiment_daily` ล้อมรอบด้วย Dimension Table 3 ตาราง คือ `dim_asset`, `dim_source`, และ `dim_date`

โครงสร้างนี้ทำให้ Dashboard Query ข้อมูลได้เร็ว เพราะ JOIN น้อยครับ"

---

## หน้า 25 — Web Dashboard Tech Stack
"Web Dashboard ใช้ Tech Stack ครับ

**FastAPI** เป็น Backend — Python framework ที่ async และ type-safe, **SQLAlchemy** เชื่อมต่อ Cloud SQL, **Jinja2** สำหรับ Server-side rendering, และ **Chart.js** สำหรับแสดงกราฟบน Browser

ทั้งหมดนี้ Deploy บน Cloud Run ครับ เข้าถึงได้ผ่าน URL สาธารณะ"

---

## หน้า 26-27 — Web Dashboard Screenshots
*[ชี้ที่ภาพ]*

"นี่คือหน้าตา Dashboard จริงครับ

มีหน้าหลักแสดงราคาสินทรัพย์และ Sentiment Index รายวัน มีกราฟ Dual-axis แสดง Price และ Sentiment ในกราฟเดียวกัน และมี Tab สำหรับดู Correlation Analysis

ทุกอย่างเชื่อมต่อ Cloud SQL แบบ Real-time ครับ"

---

## หน้า 28 — GCP Deployment
*[ชี้ที่ Diagram ขั้นตอน]*

"ขั้นตอน Deployment บน GCP ครับ

เริ่มจาก Build Docker Image บนเครื่อง → Push ขึ้น Artifact Registry → Deploy Image เป็น Cloud Run Service (Web) และ Cloud Run Job (ETL) → เชื่อมต่อกับ Cloud SQL → ตั้ง Cloud Scheduler Trigger ETL ทุกวัน

ทั้งหมดนี้ทำครั้งเดียวแล้วระบบก็ทำงานอัตโนมัติทุกวันครับ"

---

## หน้า 29 — ระบบแจ้งเตือนอีเมล
"ระบบแจ้งเตือนพัฒนาด้วย `smtplib` มาตรฐานของ Python ส่งผ่าน Gmail SMTP บนพอร์ต 465 ซึ่งเป็นการเชื่อมต่อแบบ SSL ครับ

สำหรับ Authentication ใช้ **Google App Password** แทน Password ปกติ เพื่อความปลอดภัย และเก็บ Credential ไว้ใน Cloud Run Environment Variables เพื่อไม่ให้ปรากฏในโค้ด

อีเมลที่ส่งประกอบด้วยข้อมูลครับ — วันที่รัน, สถานะ SUCCESS/FAILED, จำนวนแถวราคา, จำนวนข่าว, จำนวน Sentiment Index, ผล DQ Check และรายละเอียด Error กรณีล้มเหลว"

---

## หน้า 30 — ผลการ Deploy และทดสอบบน GCP
"เข้าสู่บทที่ 4 ผลการทดลองครับ

ผลการทดสอบระบบบน GCP ได้ผลดังนี้ครับ — HTTP Status **200 OK**, Load Time **1 นาที 14 วินาที**, ราคาที่ Upsert **258 แถว**, ข่าวที่ Insert **93 ชิ้น**, Sentiment Index **20 แถว**, DQ Check **ผ่านทุกเกณฑ์** และหน่วยความจำสูงสุดที่ใช้อยู่ที่ **3.2 GB จาก 4 GB**

ระบบทำงานได้ครบวงจรและอยู่ภายในทรัพยากรที่กำหนดครับ"

---

## หน้า 31 — Cloud Run Logs & Executions
*[ชี้ที่ภาพ]*

"นี่คือหน้าจอ Cloud Run Logs และ Job Executions จริงครับ จะเห็นว่า Job รันสำเร็จ Status เป็น Succeeded ครับ"

---

## หน้า 32 — ผลการวิเคราะห์ Sentiment ด้วย FinBERT
"ตัวอย่างผลลัพธ์จาก FinBERT ครับ

ข่าว 'Apple Reports Record Q1 Earnings, Beating Estimates' → **Positive** ด้วย Score **0.92**
ข่าว 'Tesla Reports Worst Quarter in Two Years' → **Negative** ด้วย Score **-0.87**
ข่าว 'Bitcoin Stabilizes After Recent Volatility' → **Neutral** ด้วย Score **0.05**

จะเห็นว่าโมเดลจำแนกได้ถูกต้องและ Score สอดคล้องกับความรุนแรงของข่าวครับ ข่าวชัดเจนจะได้ Score สูง ข่าวกลางๆ จะได้ Score ใกล้ 0"

---

## หน้า 33 — ผลการวิเคราะห์ความสัมพันธ์
*[ชี้ที่กราฟ Dual-axis]*

"สไลด์นี้แสดงผล Correlation Analysis ครับ

กราฟแสดง Sentiment Index และ Price Change ในแกนคู่ จะเห็นว่าระบบสร้างและแสดงผลได้แล้ว แต่ผลการวิเคราะห์เชิงสถิติยังไม่ชัดเจนในช่วงแรก เนื่องจากข้อมูลสะสมยังน้อย

โดยสรุปคือ **โครงสร้างพร้อมแล้ว** เมื่อข้อมูลสะสมมากพอ ก็สามารถนำมาวิเคราะห์ได้ทันทีโดยไม่ต้องแก้ Architecture ครับ"

---

## หน้า 34 — ผลการทดสอบ Cloud Scheduler
*[ชี้ที่ภาพ]*

"Cloud Scheduler ทดสอบสำเร็จครับ จะเห็นใน Console ว่า Job ถูก Trigger ตามเวลาที่กำหนดและ Execute สำเร็จ

นี่คือหลักฐานว่าระบบก้าวจาก 'รันได้เมื่อสั่ง' ไปสู่ 'ปฏิบัติงานอัตโนมัติด้วยตัวเอง' ซึ่งเป็นคุณสมบัติสำคัญของระบบระดับ Production ครับ"

---

## หน้า 35 — ผลการทดสอบระบบแจ้งเตือนอีเมล
*[ชี้ที่ภาพ]*

"นี่คือตัวอย่างอีเมลที่ระบบส่งอัตโนมัติหลัง ETL รันเสร็จครับ มีข้อมูลครบ — สถานะ, จำนวนข้อมูล, และผล DQ Check

ผู้ดูแลระบบรับรู้ผลการทำงานได้ทันทีโดยไม่ต้องเปิด Cloud Console ครับ"

---

## หน้า 36 — สรุปผลการทดสอบ
*[ชี้ที่ตาราง]*

"สรุปผลการทดสอบทั้งหมดครับ

เกณฑ์ทุกข้อผ่านหมดเลย — ข่าวดึงได้ 93 ชิ้น เกินเป้า 50, ไม่มีข่าวซ้ำหลุด, ข้อมูลราคา 258 แถวเกินเป้า, Sentiment Index 20 แถวเกินเป้า, เวลารัน Pipeline 1 นาที 14 วินาที เร็วกว่าเป้า 5 นาทีมาก, DQ Check ผ่านทุกเงื่อนไข และ Memory ใช้แค่ 3.2 GB จาก 4 GB ที่กำหนดครับ"

---

## หน้า 37 — 5.1 สรุปผลการดำเนินงาน
"เข้าสู่บทสุดท้าย บทที่ 5 บทสรุปครับ

โครงงานนี้บรรลุวัตถุประสงค์ครบทั้ง 5 ข้อที่ตั้งไว้ครับ

ETL Pipeline ทำงานอัตโนมัติพร้อม Fallback และ Deduplication ✅ FinBERT วิเคราะห์ Sentiment ได้ถูกต้อง ✅ Star Schema Data Warehouse พร้อม Analytical View ✅ GCP Serverless Deploy ทำงานจริงทุกวัน ✅ และ Web Dashboard เข้าถึงได้สาธารณะ ✅

ตัวเลขสำคัญจากการทดสอบ — ข่าว 93 ชิ้น, ราคา 258 แถว, เวลารัน 1 นาที 14 วินาทีครับ"

---

## หน้า 38 — 5.2 การวิจารณ์ผลลัพธ์และข้อสังเกตสำคัญ
"สำหรับผล Correlation ครับ ข้อมูลที่สะสมยังไม่เพียงพอสำหรับการสรุปผลทางสถิติที่น่าเชื่อถือ เนื่องจากระบบเพิ่งเริ่ม Deploy

แต่สิ่งสำคัญคือ **โครงสร้างพื้นฐานครบสมบูรณ์แล้ว** เมื่อข้อมูลสะสมมากขึ้น สามารถวิเคราะห์ได้ทันทีโดยไม่ต้องเปลี่ยน Architecture

ส่วนปัญหาที่พบระหว่าง Development ครับ — Error 403 จาก IAM ขาด Permission, Error 500 จากไม่มี View ใน DB, Email ส่งไม่ได้เพราะใช้ Password ผิดประเภท, และ Docker Build บน ARM64 ไม่รันบน Cloud AMD64

ทุกปัญหามีวิธีแก้ไขชัดเจนและแก้ได้หมดครับ"

---

## หน้า 39 — 5.3 ข้อจำกัดของระบบ
"ข้อจำกัดของระบบในปัจจุบันมี 5 ข้อครับ

หนึ่ง แหล่งข้อมูล Free Tier มี Rate Limit, สอง Batch Processing วันละครั้ง ไม่รองรับ Intraday, สาม ครอบคลุมแค่ 7 สินทรัพย์ ยังไม่มีหุ้นไทย, สี่ Secrets เก็บใน Environment Variables ยังไม่ใช้ Secret Manager, และห้า ข้อมูลสะสมยังน้อย

ข้อจำกัดเหล่านี้เป็น Known Trade-off ที่ยอมรับได้ในระดับ Academic Project และสามารถแก้ไขได้ในการพัฒนาต่อยอดครับ"

---

## หน้า 40 — 5.4 ข้อเสนอแนะสำหรับการพัฒนาต่อยอด
"แนวทางพัฒนาต่อยอดแบ่งเป็น 3 ระยะครับ

**ระยะสั้น ภายใน 3 เดือน** — ย้าย Secrets ไป Secret Manager, เพิ่ม Backfill Historical Data, และเพิ่มแจ้งเตือน Line/Slack

**ระยะกลาง 3–12 เดือน** — เปลี่ยนแหล่งข้อมูลเป็น Paid API ที่มี SLA, แยก FinBERT เป็น Microservice บน Vertex AI, และขยายสู่หุ้น SET ไทย

**ระยะยาว เกิน 1 ปี** — พัฒนา Real-time Streaming ด้วย Pub/Sub, Fine-tune FinBERT สำหรับภาษาไทย, และสร้าง Predictive Model ที่ใช้ Sentiment ร่วมกับ Technical Indicators ครับ"

---

## หน้า 41 — 5.5 บทเรียนที่ได้รับ
"บทเรียนสำคัญ 5 ข้อจากโครงงานนี้ครับ

หนึ่ง **Cloud Architecture ต้องออกแบบตั้งแต่ต้น** — IAM, Connectivity, Platform Compatibility ไม่ใช่รายละเอียดที่ค่อยแก้ทีหลังได้

สอง **Idempotency คือหัวใจของ ETL ที่เชื่อถือได้** — UPSERT + UNIQUE Constraint ทำให้ Rerun กี่ครั้งก็ไม่เสียหาย

สาม **งาน AI/ML ต้องวางแผนทรัพยากรรอบคอบ** — FinBERT ใช้ Memory สูง การแยก Dockerfile เป็นตัวอย่างที่ดี

สี่ **Security ไม่ใช่เรื่องที่ค่อยมาเพิ่มทีหลัง** — ปัญหา Gmail Auth ส่งผลตรงต่อระบบ

และห้า **ระบบ End-to-End ให้บทเรียนมากกว่าการทดลองแยกส่วน** — การเชื่อมทุก Layer ตั้งแต่ Ingestion จนถึง Alerting ทำให้เข้าใจ Production System อย่างแท้จริงครับ"

---

## หน้า 42 — 5.6 สรุป
"สรุปโดยรวมครับ โครงงานนี้ได้พัฒนาระบบ **Automated Data Pipeline และ Data Warehouse** ครบวงจร โดยบูรณาการ ETL Pipeline, FinBERT NLP Model, Star Schema Data Warehouse, และ GCP Serverless Infrastructure เข้าด้วยกันเป็นระบบที่ทำงานอัตโนมัติทุกวัน

จุดเด่นคือเป็นระบบ Production-grade ที่ Deploy และทำงานจริงบน Google Cloud Platform ครอบคลุมทุก Layer ของ Data Engineering และออกแบบด้วยหลัก Idempotency, Fault Tolerance, และ Serverless-First

และเมื่อข้อมูลสะสมเพียงพอแล้ว ผมได้นำมาวิเคราะห์จริง ซึ่งคำตอบเป็นผลลบครับ

ตอนที่มีสินทรัพย์ข้อมูลครบเกณฑ์เพียง 5 ตัว แบบจำลองให้ความแม่นยำ 63.5% เทียบเส้นฐาน 55.8% แต่เมื่อขยายเป็น 29 ตัว กลายเป็น 48.3% เทียบเส้นฐาน 59% และไม่ชนะเส้นฐานเลย ความได้เปรียบเดิมเป็นความผันผวนจากกลุ่มตัวอย่างขนาดเล็ก

เมื่อตรวจซ้ำด้วย AUC การเพิ่ม sentiment ทำให้ค่าลดลงทุกขอบเขตเวลา และค่าสหสัมพันธ์ที่มีนัยสำคัญสูงส่วนใหญ่หายไปเมื่อวัดด้วยอันดับ

ผลนี้สอดคล้องกับสมมติฐานตลาดมีประสิทธิภาพรูปแบบกึ่งเข้ม คุณูปการของโครงงานจึงเป็นโครงสร้างข้อมูลและระเบียบวิธีที่ทำให้สรุปได้อย่างมั่นใจว่าไม่พบความสัมพันธ์ครับ"

---

## หน้า 43 — Thank You
"ขอบคุณครับ ขอบคุณ ผศ.ดร.กนกวรรณ อัจฉริยะชาญวณิช อาจารย์ที่ปรึกษา และคณาจารย์คณะเทคโนโลยีสารสนเทศ สจล. ทุกท่านครับ

ถ้ามีคำถามหรือข้อสงสัยใด ยินดีตอบครับ"

---

## 💡 Tips การพรีเซนต์
- พูดช้าๆ ชัดๆ อย่ารีบ — 20 นาที มีเวลาพอ
- สไลด์ภาพ/Diagram → ชี้และอธิบาย อย่าอ่านตาม
- ถ้าลืม → ดูที่สไลด์แล้วเริ่มใหม่ ไม่ต้องขอโทษ
- Q&A → ถ้าไม่แน่ใจตอบว่า "ข้อนี้ยังไม่ได้ทดสอบครับ แต่ในทฤษฎีคือ..." ดีกว่าตอบผิด
