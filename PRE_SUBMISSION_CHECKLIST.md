# รายการตรวจก่อนส่ง / ก่อนนำเสนอ

จัดทำ 2026-09-05 หลังการตรวจสอบเอกสารทั้งหมดเทียบกับระบบและผลการวิจัยปัจจุบัน

รายการในหมวด 🔴 คือสิ่งที่**ผมแก้ให้ไม่ได้** เพราะไม่ทราบว่าอะไรถูกต้อง
ส่วนหมวด ✅ แก้ให้แล้วในไฟล์ที่ระบุ

---

## ✅ แก้แล้ว — ชื่อผู้จัดทำ

ยืนยันโดยผู้จัดทำเมื่อ 2026-09-05: **นายเอเชีย อ่อนพรม รหัส 66070324**

`presentation_slides.md` เคยระบุเป็น "นายอาซิส อิลยาส" (1 จุด) แก้แล้ว ตอนนี้ทุก
ไฟล์ Markdown และไฟล์ `.docx` ทั้งสองใช้ชื่อเดียวกัน

พร้อมกันนี้ได้เติมช่องที่ยังเป็น placeholder ในหน้าปกและหน้าอนุมัติ โดยใช้ค่าที่
ปรากฏอยู่แล้วในเอกสารอื่นของโครงงานเดียวกัน — **ควรตรวจยืนยันอีกครั้ง**

| ช่อง | ค่าที่เติม | ที่มา |
|---|---|---|
| อาจารย์ที่ปรึกษา | ผศ.ดร.กนกวรรณ อัจฉริยะชาญวณิช | `slide_details.md` |
| สาขาวิชา | วิทยาการข้อมูลและการวิเคราะห์เชิงธุรกิจ | `slide_details.md` |
| คณะ | เทคโนโลยีสารสนเทศ | `slide_details.md` |

ตรวจซ้ำด้วย: `grep -rn "66070324\|อ่อนพรม\|กนกวรรณ" *.md`

---

## 🔴 ต้องทำเองก่อนนำเสนอ — ภาพหน้าจอ

URL เดิมบน Cloud Run (`fin-web-nnebdwza6q-as.a.run.app`) **ตอบ HTTP 500** เพราะ
billing ถูกปิดและ Cloud SQL อยู่สถานะ `SUSPENDED` ทุกไฟล์ถูกแก้ให้ชี้ไปที่
`http://localhost:8000` แล้ว แต่ต้องเปิดระบบเองก่อนแคปหน้าจอ

```bash
docker start fin-postgres
python run_web.py
```

ภาพที่ควรแคปเพิ่ม เพราะเป็นคำตอบต่อข้อสังเกตของคณะกรรมการ
- แท็บ **Signal** ส่วน **Volatility outlook** — สำคัญที่สุด เป็นผลบวกรายการเดียว
  พร้อมตารางหลักฐาน skill เทียบ persistence (`?tab=predict&ticker=NVDA`)
- แท็บ **Signal** — ส่วน backtest, เส้นฐาน, walk-forward และ SHAP
- แท็บ **Fundamentals** — กราฟรายได้รายไตรมาสและตาราง
- แท็บ **Correlations** — ให้เห็นแถบสีเหลืองที่กำกับว่าเป็นหลักฐานประกอบ

---

## 🟡 ควรทบทวน — เรื่องเล่าของการนำเสนอเปลี่ยนไป

สไลด์และสคริปต์เดิมวางเรื่องเล่าไว้ว่า *"สร้างระบบเสร็จแล้ว รอข้อมูลเพื่อสรุปผล"*
ซึ่งเล่าไม่ได้อีกแล้ว เพราะข้อมูลมาแล้วและผลเป็นลบ

เรื่องเล่าที่แนะนำ ซึ่งแข็งแรงกว่าในเชิงวิชาการ

1. **ปัญหา** — ข่าวการเงินมีมหาศาล มนุษย์ตามไม่ทัน
2. **ระบบ** — ETL อัตโนมัติ + FinBERT + Data Warehouse + Dashboard
3. **ผลแรกที่ดูดี** — ความแม่นยำ 63.5% เทียบเส้นฐาน 55.8% จากสินทรัพย์ 5 ตัว
4. **การตรวจสอบ** — ขยายเป็น 29 ตัว ตัวเลขกลายเป็น 48.3% ไม่ชนะเส้นฐานเลย
5. **ตรวจซ้ำอีกชั้น** — AUC ลดลงทุกขอบเขต, Pearson ที่ p<0.001 หายเมื่อวัดด้วยอันดับ,
   ขนาดตัวอย่างที่แท้จริงเล็กกว่าจำนวนแถวหลายเท่า
6. **แล้วอะไรที่ทำนายได้** — วัดคุณสมบัติข้อมูลตรงๆ พบว่าทิศทางมี autocorrelation
   −0.05 แต่ความผันผวนมี +0.90 จึงเปลี่ยนคำถาม และได้ผลบวก: ชนะเส้นฐาน
   persistence 5/5 folds และ 29/30 สินทรัพย์
7. **ข้อสรุป** — ไม่ใช่ "ทำนายอะไรไม่ได้" แต่เป็น **"ทิศทางทำนายไม่ได้ ขนาดของ
   การเคลื่อนไหวทำนายได้"** และผลบวกยังทำหน้าที่เป็น positive control พิสูจน์ว่า
   ระบบวัดตรวจเจอสัญญาณได้เมื่อมีสัญญาณจริง

> **จุดที่ควรเน้นตอนพรีเซนต์**: ลำดับ 3 → 4 → 5 → 6 คือเรื่องเล่าทั้งหมด —
> เจอตัวเลขที่ดูดี ตรวจแล้วพบว่าไม่จริง ตรวจซ้ำอีกชั้น แล้วหาคำถามที่ข้อมูล
> ตอบได้จริง กรรมการให้น้ำหนักกับกระบวนการนี้มากกว่าตัวเลขปลายทาง

จุดแข็งของเรื่องเล่านี้คือแสดงให้เห็นว่า**ผู้วิจัยตรวจสอบผลของตัวเองจนพบว่าผิด**
ซึ่งเป็นสิ่งที่กรรมการให้น้ำหนักมากกว่าตัวเลขสวยๆ ที่ไม่ได้ตรวจสอบ

---

## ✅ แก้ให้แล้ว

| ประเด็น | เดิม | ปัจจุบัน | ไฟล์ |
|---|---|---|---|
| จำนวนสินทรัพย์ | 7 | 30 | ทุกไฟล์ |
| หน้าต่างข่าว | 168 ชม. | 2,160 ชม. | `chapter2`, `chapter345`, สคริปต์ |
| แท็บ Dashboard | 5 (มี Heatmap) | 7 (ถอด Heatmap เพิ่ม Signal, Fundamentals) | ทุกไฟล์ |
| คำค้นต่อสินทรัพย์ | คำบรรยายยาว | ชื่อเรียกมาตรฐาน พร้อมตัวเลขวัดผล | `chapter345`, `report_full` |
| สถานะ GCP | ทำงานอยู่ | ระบุว่าทดสอบผ่านแล้วแต่หยุดทำงาน | `chapter345`, `report_full` |
| ผล Correlation | "รอข้อมูลแล้วจะสรุปได้" | ผลจริง (เป็นลบ) พร้อมตัวเลข | ทุกไฟล์ |
| URL เว็บ | Cloud Run (ตาย) | `localhost:8000` | ทุกไฟล์ |
| จำนวนตาราง | fact 3 / dim 3 | fact 6 / dim 4 | `chapter345` |

---

## 📄 เอกสารที่ยังไม่ได้อัปเดต

`Financial_Sentiment_ETL_Thesis.docx` และ `Financial_Sentiment_ETL_Thesis_v2_Formatted.docx`
ยังเป็นเนื้อหาเวอร์ชันเดิม ต้องนำเนื้อหาจากไฟล์ Markdown ที่แก้แล้วไปรวมตอนจัดรูปเล่ม

ลำดับที่แนะนำ
1. `chapter2_detailed.md` + `chapter345_detailed.md` — เนื้อหาหลัก แก้แล้ว
2. `thesis_methodology_revised.md` — ระเบียบวิธี ผลการทดลอง และข้อจำกัดฉบับปรับปรุง
3. `DATA_DECISIONS.md` — ที่มาของตัวเลขทุกตัว ใช้อ้างอิงเมื่อกรรมการถาม

---

## 🔍 คำถามที่ควรเตรียมคำตอบ

**"ทำไมผลถึงเป็นลบ ระบบมีปัญหาหรือเปล่า"**
ปัจจัยหลักคือการเปลี่ยนสภาวะตลาดระหว่างชุดฝึกกับชุดทดสอบ (สัดส่วนวันขึ้น 0.438 → 0.590)
ไม่ใช่คุณภาพของ FinBERT หรือของ pipeline ข้อมูล sentiment มีเพียง 9 เดือนจึงฝึกข้าม
สภาวะตลาดไม่ได้

**"n = 524 ก็เยอะแล้วไม่ใช่หรือ"**
524 แถวมาจากวันตลาดเพียง 33 วัน เพราะแถวจาก 29 สินทรัพย์ในวันเดียวกันเคลื่อนไหว
ตามตลาดพร้อมกัน ขนาดตัวอย่างที่แท้จริงคือจำนวนวัน ระบบรายงานทั้งสองค่าแล้ว

**"ทำไมไม่ลองโมเดลที่ซับซ้อนกว่านี้"**
ทดลองแล้วทั้ง Logistic Regression และ Gradient Boosting ผลใกล้เคียงกัน และค่า AUC
ที่ประมาณ 0.5 บ่งชี้ว่าไม่มีข้อมูลให้จับ ไม่ใช่ว่าจับไม่ได้ การเปลี่ยนโมเดลไม่แก้ปัญหา
regime shift

**"ผลลบแบบนี้จบโครงงานได้หรือ"**
งานนี้ไม่ได้มีแต่ผลลบ — Track C พยากรณ์ความผันผวนได้จริงและชนะเส้นฐาน 5/5 folds
ส่วนผลลบของ Track A และ B สอดคล้องกับสมมติฐานตลาดมีประสิทธิภาพรูปแบบกึ่งเข้ม
ซึ่งเป็นทฤษฎีกระแสหลัก และการมีผลบวกอยู่ด้วยทำให้ผลลบน่าเชื่อถือขึ้น เพราะพิสูจน์
ว่าระบบวัดตรวจเจอสัญญาณได้เมื่อมีสัญญาณจริง พร้อมชุดทดสอบอัตโนมัติ 83 รายการและ
บันทึกการตัดสินใจทุกข้อใน `DATA_DECISIONS.md`

**"เจอบั๊กอะไรบ้างระหว่างทาง"**
7 จุดที่ให้ผลดูปกติโดยไม่มี error — เครื่องหมาย `&` ในคำค้นทำให้ ^GSPC ค้นหาตัวอักษร
"S", การจับคู่งบการเงินกับราคาผิดวัน 39% ของชุดข้อมูล, อัตราส่วนที่ตัวหารเกือบศูนย์
ทำให้ R² ตกไปที่ −34, Oracle แท็กรายได้เป็น 0 ทำให้ correlation ทั้งคอลัมน์เป็น NaN,
restart policy ของฐานข้อมูลไม่ตรงกับส่วนอื่น, หน้าต่างเวลาแบบตายตัวที่ทำให้ช่องว่าง
ข้อมูลกลายเป็นถาวร และ cache-busting ที่อิงไฟล์ CSS อย่างเดียวทำให้คำแปลภาษาไทยที่
แก้ไปไม่เคยขึ้น ทั้งหมดเจอจากการตรวจตัวเลขที่อธิบายไม่ได้ ไม่ใช่จากการอ่านโค้ด

---

## ✅ เพิ่มเมื่อ 2026-09-05 — ผลการพยากรณ์ความผันผวน (Track C)

เขียนลงเอกสารครบแล้วในไฟล์ต่อไปนี้

| ไฟล์ | ส่วนที่เพิ่ม |
|---|---|
| `thesis_methodology_revised.md` | §3.8 วิธีการ, §4.3 ผล, §4.4 สรุปใหม่, §5.2 ข้อจำกัดหน้าต่างซ้อนทับ |
| `chapter345_detailed.md` | §4.3.1 ผลเต็ม, §5.1 ผลลัพธ์เพิ่มเติม, §5.6 สรุปใหม่ |
| `presentation_slides.md` | Slide 13b ใหม่ทั้งสไลด์ + Slide 18 สรุปใหม่ |
| `script_25min.md`, `script_presentation.md` | บทพูดปิดท้าย |
| ไฟล์นำเสนอทั้ง 7 | แถบเตือนหัวไฟล์ |

**ตัวเลขหลักที่ต้องจำให้แม่นตอนพรีเซนต์**

- autocorrelation: ทิศทาง **−0.053** vs ความผันผวน **+0.901** (ต่ำสุดใน 30 ตัวยัง +0.821)
- skill: **+0.213** (5 วัน) และ **+0.339** (21 วัน)
- ชนะเส้นฐาน **5/5 folds** ทั้งสองช่วงเวลา และ **29/30 สินทรัพย์**
- ค่าคลาดเคลื่อนกลาง 4.67% เทียบเส้นฐาน 5.78% ต่อปี (21 วัน)

**ประโยคที่ต้องพูดให้ชัด**: เกณฑ์วัดผลไม่ใช่ R² แต่เป็น **skill เทียบ persistence**
เพราะการทำนายความผันผวนจากความผันผวนได้ R² สูงโดยโครงสร้างอยู่แล้ว ถ้ากรรมการ
เห็นแค่ 0.739 แล้วคิดว่าดีเกินจริง ต้องอธิบายจุดนี้ทันที
