# รายงานโครงงาน: ระบบสกัดและวิเคราะห์ความรู้สึกข่าวการเงินด้วย FinBERT บน Google Cloud Platform (GCP)

> ## ⚠️ ปรับปรุง 5 ก.ย. 2026 — อ่านก่อนใช้
>
> ไฟล์นี้เขียนเมื่อ ก.ค. 2026 เนื้อหาบางส่วนไม่ตรงกับระบบและผลการวิจัยปัจจุบัน
>
> | ประเด็น | เดิม | ปัจจุบัน |
> |---|---|---|
> | URL เว็บบน Cloud Run | ใช้งานได้ | **ตอบ HTTP 500** — แคปหน้าจอจาก `http://localhost:8000` แทน |
> | จำนวนสินทรัพย์ | 7 | **30** |
> | หน้าต่างข่าว | 168 ชม. (7 วัน) | **2,160 ชม. (90 วัน)** |
> | แท็บ Dashboard | 5 (มี Heatmap) | **7** (ถอด Heatmap เพิ่ม Signal, Fundamentals) |
> | ผล Correlation | "รอข้อมูลสะสมแล้วจะสรุปได้" | **ข้อมูลพอแล้ว — ผลเป็นลบ** |
>
> ข้อสรุปปัจจุบัน: ทั้ง sentiment และงบการเงิน **ไม่แสดงความสัมพันธ์กับผลตอบแทน**
> ที่รอดการตรวจสอบ 3 ชั้น (ค่าผิดปกติ / นอกกลุ่มตัวอย่าง / ขนาดตัวอย่างจริง)
> รายละเอียดใน `thesis_methodology_revised.md`

---


---

## บทที่ 3: วิธีการดำเนินงานโครงงาน (System Architecture & Methodology)

### 3.1 ภาพรวมของสถาปัตยกรรมระบบ (System Architecture Overview)
ระบบถูกออกแบบโดยใช้สถาปัตยกรรมแบบ Microservices และคอนเทนเนอร์ (Containerization) เพื่อให้สามารถแยกส่วนการทำงานและดูแลรักษาได้ง่าย โดยแบ่งการทำงานออกเป็น 3 ส่วนหลัก ได้แก่:
1. **ETL Pipeline (Backend Processing):** ดึงข้อมูลข่าวและราคาหุ้นรายวัน วิเคราะห์ความรู้สึกด้วย FinBERT และเก็บลงฐานข้อมูล
2. **Web Dashboard (Frontend & API):** นำเสนอข้อมูลสถิติและความรู้สึกแบบกราฟิกผ่าน Web Interface
3. **Database (Storage):** จัดเก็บข้อมูลทั้งหมดในรูปแบบ Star Schema

*(เว้นที่ไว้ประมาณครึ่งหน้า)*
**[แทรกรูปภาพที่ 1: ภาพวาด Diagram หรือ Flowchart ของ System Architecture ทั้งระบบ (Airflow/Cloud Scheduler -> ETL -> Cloud SQL -> Web Dashboard)]**

### 3.2 การออกแบบโครงสร้างฐานข้อมูล (Database Design)
ระบบใช้ฐานข้อมูลแบบเชิงสัมพันธ์ (Relational Database) โดยออกแบบในรูปแบบ **Star Schema** เพื่อรองรับการสืบค้นข้อมูลเชิงวิเคราะห์ (OLAP) ได้อย่างรวดเร็ว ประกอบด้วย:
- **Fact Table (`fact_sentiment_price`):** เก็บข้อมูลความรู้สึก (Sentiment Score) ที่วิเคราะห์ได้ พร้อมกับข้อมูลราคาหุ้น (Open, Close, High, Low) ในแต่ละวัน
- **Dimension Tables:** 
  - `dim_assets`: เก็บรายชื่อสินทรัพย์ (Tickers) เช่น AAPL, TSLA, BTC-USD
  - `dim_source`: เก็บแหล่งที่มาของข่าว และคะแนนความน่าเชื่อถือ (Credibility Score)
  - `dim_dates`: ตารางมิติเวลาสำหรับการนำเสนอข้อมูลแบบ Time-series

*(เว้นที่ไว้ประมาณครึ่งหน้า)*
**[แทรกรูปภาพที่ 2: ภาพ ER-Diagram หรือ Schema ของฐานข้อมูล fin_dw]**

### 3.3 การวิเคราะห์ความรู้สึกด้วยโมเดล FinBERT (Sentiment Analysis)
ระบบใช้โมเดลปัญญาประดิษฐ์ทางการเงิน (FinBERT) ซึ่งพัฒนาขึ้นจากสถาปัตยกรรม BERT ที่ถูกเทรน (Pre-trained) ด้วยคลังข้อมูลทางการเงินโดยเฉพาะ ทำให้สามารถจำแนกอารมณ์ของข่าวได้ 3 ระดับ ได้แก่:
- **Positive (เชิงบวก):** ข่าวที่ส่งผลดีต่อราคาหรือสะท้อนปัจจัยบวก
- **Negative (เชิงลบ):** ข่าวที่สะท้อนปัจจัยลบ อัตราเงินเฟ้อ หรือการขาดทุน
- **Neutral (เป็นกลาง):** ข่าวเชิงข้อเท็จจริงทั่วไป

กระบวนการทำงานคือ การดึงบทสรุปข่าว (RSS Feed) เข้าสู่ PyTorch Pipeline ด้วยเซ็ตคำสั่ง `transformers` เพื่อทำการรัน Inference และได้ผลลัพธ์ค่าน้ำหนัก (Probability) นำมาเป็นคะแนน Sentiment Score 

*(เว้นที่ไว้ประมาณ 1 ใน 4 หน้า)*
**[แทรกรูปภาพที่ 3: ภาพตัวอย่างโค้ดหรือ Flow การทำ Sentiment Scoring ด้วย FinBERT]**

---

## บทที่ 4: การนำระบบขึ้นติดตั้งบนคลาวด์ (Cloud Deployment & Implementation Results)

ระบบทั้งหมดถูกย้ายจากการทำงานบนเครื่องส่วนบุคคล (Local Environment) ขึ้นไปทำงานบน **Google Cloud Platform (GCP)** แบบเต็มรูปแบบ (Serverless Architecture) เพื่อเพิ่มความเสถียรภาพ ขจัดปัญหาคอขวดของฮาร์ดแวร์ และรองรับผู้ใช้งานจริง

### 4.1 การเตรียมความพร้อมของ Container (Artifact Registry)
เพื่อให้ระบบรันได้อย่างเป็นอิสระจากแพลตฟอร์ม (Platform-agnostic) โค้ดถูกสร้างเป็น Docker Images 2 ส่วน คือ `fin-web` (ระบบแสดงผล) และ `fin-etl` (ระบบดึงข้อมูลและวิเคราะห์) 
ภาพ (Image) ทั้งสองถูกคอมไพล์ด้วยสถาปัตยกรรมแบบ `linux/amd64` ตามมาตรฐานของ Cloud Run และถูกจัดเก็บที่ **Google Artifact Registry**

*(เว้นที่)*
**[แทรกรูปภาพที่ 4: ภาพหน้าจอ GCP Artifact Registry ที่แสดงรายชื่อ Image fin-web และ fin-etl]**

### 4.2 ระบบฐานข้อมูลบนคลาวด์ (Cloud SQL)
ข้อมูลทั้งหมดถูกเชื่อมไปยัง **Google Cloud SQL** ชนิด PostgreSQL 16 ซึ่งช่วยในการจัดการ สำรองข้อมูล และรักษาความปลอดภัยขั้นสูง (Managed Database) 
ตัว Web Dashboard และ ETL Pipeline เชื่อมต่อเข้าสู่ฐานข้อมูลโดยตรงผ่าน Native Unix Sockets ของ Cloud Run ทำให้การรับส่งข้อมูลมีความรวดเร็วและปลอดภัย

*(เว้นที่)*
**[แทรกรูปภาพที่ 5: ภาพหน้าจอ Google Cloud SQL Console ที่แสดง Instance "fin-sentiment-db"]**

### 4.3 ระบบ Web Dashboard (Cloud Run Services)
หน้าจอแสดงผลถูกติดตั้งบนบริการ **Cloud Run** ซึ่งทำหน้าที่เป็น Web Server แบบ Serverless ระบบจะปรับขยาย (Scale) อัตโนมัติตามปริมาณการเข้าชม และมอบ URL สาธารณะ ให้ผู้ใช้งานสามารถเข้ามาเรียกดูวิเคราะห์แนวโน้มตลาดได้ทุกที่ทุกเวลาโดยไม่ต้องมีเซิร์ฟเวอร์ตั้งอยู่ที่บ้าน

*(เว้นที่)*
**[แทรกรูปภาพที่ 6: ภาพหน้าจอ Web Dashboard หรือ URL สาธารณะของ Cloud Run ที่เปิดบน Web Browser (มีกราฟและตาราง)]**

### 4.4 ระบบดึงข้อมูลอัตโนมัติ (ETL & Cloud Run Jobs + Cloud Scheduler)
เพื่อให้ระบบมีข้อมูลเป็นปัจจุบันเสมอ ไพป์ไลน์ถูกตั้งค่าให้รันแบบอัตโนมัติ:
1. การดึงข้อมูลและ AI จะถูกรันบน **Cloud Run Jobs** โดยมีการจัดสรรหน่วยความจำจำนวน 4GB เพื่อรองรับการประมวลผล PyTorch ของ FinBERT
2. การรันแบบอัตโนมัติ ถูกสั่งการโดย **Google Cloud Scheduler** (ตั้งเวลา Cron expression เป็น `0 8 * * *` โซน Asia/Bangkok) ทำให้ตัว Job ทำงานเองทุกวันเวลา 8:00 น. โดยไม่ต้องใช้คนคอยกด

*(เว้นที่)*
**[แทรกรูปภาพที่ 7: ภาพหน้าจอ Cloud Scheduler ที่รันสำเร็จ (Status: Success)]**
**[แทรกรูปภาพที่ 8: ภาพหน้าจอ Logs หรือประวัติการรันของ Cloud Run Job (fin-etl-job) ที่ขึ้นเครื่องหมายติ๊กถูก]**

### 4.5 ระบบการแจ้งเตือนและการจัดการข้อผิดพลาด (Email Alerting System)
เพื่อให้ผู้ดูแลหน้าเว็บ (Admin) มั่นใจได้ว่าข้อมูลรายวันถูกดึงเรียบร้อย ระบบมีการแจ้งเตือนอัตโนมัติผ่านโปรโตคอล **SMTP (Simple Mail Transfer Protocol)** จากบัญชีอีเมลระบบ ไปยังอีเมลของผู้พัฒนา (66070324@kmitl.ac.th)
การแจ้งเตือนนี้ไม่ได้ใช้รหัสผ่านทั่วไป แต่ถูกควบคุมความปลอดภัยด้วย **Google App Password** ที่ซ่อนรหัสผ่านไว้ใน Environment Variable ของบัญชี Cloud อย่างแน่นหนา ตัวอีเมลจะปรากฏสรุปสินทรัพย์ที่ดึงผ่าน แหล่งข่าวที่รวบรวมได้ และข้อความแจ้งเตือน (Errors/Rate Limits) หากมีปัญหา

*(เว้นที่)*
**[แทรกรูปภาพที่ 9: ภาพหน้าจอ Screenshot จาก Inbox ของอีเมลที่ได้รับ Alert รายวันจากระบบ]**

---

## บทที่ 5: สรุปผลและข้อเสนอแนะ (Conclusion & Future Work)

### 5.1 สรุปผลการดำเนินงาน
โครงงานนี้ประสบความสำเร็จในการออกแบบ สร้าง และติดตั้งระบบกวาดข้อมูลข่าวสารทางการเงิน พร้อมทั้งวิเคราะห์ความรู้สึกที่มีต่อสินทรัพย์นั้นอย่างรวดเร็วด้วย FinBERT กระบวนการทั้งหมดทำงานแบบ Serverless บน Google Cloud Platform (GCP) ซึ่งทำให้ระบบมีความทนทาน (Fault Tolerant) และสามารถแสดงข้อมูลให้แก่ผู้ใช้งานได้ทุกวัน 

### 5.2 ข้อเสนอแนะสำหรับการพัฒนาต่อยอด (Future Work & Limitations)
แม้ว่าผลการทดสอบระบบจะตอบโจทย์เป้าหมายการวิเคราะห์อารมณ์ในตลาดได้อย่างดีเยี่ยม แต่หากนำไปปรับใช้จริงในระดับองค์กร (Production Scale) ระบบยังมีจุดที่ควรได้รับการพัฒนาต่อเติมดังนี้:
1. **เพิ่มแหล่งข้อมูลทางการ (Premium Data Source):** ปัจจุบันการดึงราคาใช้ Yahoo Finance ทำให้พบปัญหา `RateLimitError` ควรเปลี่ยนไปใช้ API ข้อมูลหลักทรัพย์เช่น Bloomberg, Settrade, หรือ Alpha Vantage แบบมีค่าใช้จ่าย
2. **ปรับกระบวนการวิเคราะห์เป็นแบบ Real-Time Streaming:** ควรสลับจากการรันวันละ 1 รอบ (Batch Processing) เป็นการตรวจจับข่าวสดและส่งวิเคราะห์ด้วย Kafka หรือ Google Pub/Sub ในหน่วยวินาที เพื่อให้ข้อมูลสะท้อนความผันผวนของตลาดได้แม่นยำที่สุด
3. **การแยกโมเดล AI ออกจาก ETL:** เพื่อให้อินฟราสตรัคเจอร์มีความรวดเร็วขึ้น ควรพิจารณานำโมเดล FinBERT ย้ายไปทำเป็น Microservices เดี่ยว ๆ (เช่น Vertex AI Endpoint) เพื่อไม่ให้โหลดงานในฝั่ง Data Processing มากเกินไป และสามารถเรียกใช้งาน GPU (Graphics Processing Unit) ให้ทำงานได้เร็วยิ่งขึ้น
